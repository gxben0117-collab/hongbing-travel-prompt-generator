# 紅兵旅拍大片生成器

This project was created from the provided source file:

`紅兵旅拍大片生成咒語產生器_v1.47.html`

## Project Files

- `index.html` - the current single-page application, including styles, data, and browser logic.
- `package.json` - optional local preview scripts.

## Run Locally

Open `index.html` directly in a browser, or run:

```bash
npm install
npm run dev
```

Then open the local URL shown in the terminal.

## Documentation

Project documents and versioned development logs are in `docs/`.

## Notes

The first project pass keeps the original HTML intact so behavior is preserved. Future cleanup can split the file into separate CSS, JavaScript, and data modules.
