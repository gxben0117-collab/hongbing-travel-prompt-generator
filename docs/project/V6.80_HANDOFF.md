# V6.80 Handoff

Date: 2026-05-20

## Status

`v6.80` is the current clean baseline for the travel prompt generator after another narrow duplicate-variant pruning pass.

Use this as the base for future work:

- `src/data.js`
- `index.template.html`
- `dist/index.html`
- `index.html`
- `versions/index_v6.80.html`

## Change Summary

This pass removes:

- `ref_castle_wedding_princess`

Reason:

It overlapped too much with the remaining bridal hero references while contributing less travel-specific uniqueness than the versions kept in the library.

## Validation

Run:

```powershell
node --check src/data.js
node --check src/app.js
node scripts/build.mjs
```

Current results:

- all checks passed
- build passed

## Integrity

`dist/index.html`, `index.html`, and `versions/index_v6.80.html` share the same SHA256:

```text
5EDA61AA69A09392D14B9697E5B1F25A0F3A00E307A9C9C9E50B7A66667AD711
```

## Next Likely Step

Continue with one narrow pruning pass on:

- `fallen_angel`
- `tang`

Keep the same strategy:

- one project only
- one hard deliverable per pass
- build and snapshot after every removal
